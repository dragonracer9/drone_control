#if defined(ARDUINO) && ARDUINO >= 100 // arduino.h is only included if the arduino version is 1.0 or higher (//FIXME: chack if version is correct for nano)
#include "Arduino.h"
#endif

#include "control.h"
#include "WIFI.h"

